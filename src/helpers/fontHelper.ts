import { Creepster } from "next/font/google";

export const fontCreepster = Creepster({
  variable: "--font-creepster",
  subsets: ["latin"],
  display: "swap",
  weight: ["400"],
  preload: true,
});
